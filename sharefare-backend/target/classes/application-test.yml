spring:
  datasource:
    url: jdbc:h2:mem:sharefare;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false

app:
  jwt:
    secret: test-test-test-test-test-test-test-test
    ttlSeconds: 3600

