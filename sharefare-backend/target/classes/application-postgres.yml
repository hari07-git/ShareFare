spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/sharefare}
    username: ${DB_USER:sharefare}
    password: ${DB_PASSWORD:sharefare}
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
    open-in-view: false

